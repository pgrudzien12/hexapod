#include "whole_body_control.h"

void whole_body_control_compute(const swing_trajectory_t *trajectory, whole_body_cmd_t *cmds) {
    // Convert desired foot positions to joint angles (IK placeholder)
}
